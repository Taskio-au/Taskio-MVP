# Firebase Functions transactional email (names / placeholders only).
# Safe default: do not send. Do not put real credentials in this file.

EMAIL_ENABLED=false
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
CHAT_EMAIL_FROM=
MAIL_FROM=
TASKIO_APP_URL=
