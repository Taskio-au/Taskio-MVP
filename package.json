{
  "name": "taskio-security-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "firebase emulators:exec --project demo-taskio-rules --only firestore,storage \"node --test --test-concurrency=1 rules-tests/security.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.1",
    "firebase-tools": "^15.27.0"
  },
  "engines": {
    "node": ">=20"
  }
}
